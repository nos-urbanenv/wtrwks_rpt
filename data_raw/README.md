# 解析データ入力場所

このフォルダには、機器分析の結果とExcelファイルが入っている。

機器分析の結果は、出力されたものをそのままのファイル形式で格納している。

Excelファイルの中身は、実験ノートから転記した生データである。
githubでdiffが追跡できるように、Quartoに埋め込んだスクリプトで、Excelファイル内のシートはcsv形式に変換し、/dataフォルダに格納されるようになっている。